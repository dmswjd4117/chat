<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        link(rel="stylesheet", href="/css/slack.css")
        link(rel="shortcut icon", href="/image/favicon.png", type="image/x-icon")
        <script src="https://kit.fontawesome.com/00307318ee.js" crossorigin="anonymous" defer></script>
        
        script( src="/socket.io/socket.io.js" defer)
        script(src="/js/joinNamespace.js" defer)
        script( src="/js/chat.js" defer)
        title slack
    body
        section(class="main")
            div(id="popup-hide" class="add-spaceroom-box") 
                button(class="popup-close") X
                div(class="add-form") 
                    label space name  
                    input(type="text" name="name"  class="space-name-input" required)  
                    button(class="add-spaceroom-btn") 채널 만들기

            section(class="namespace")
                //-   네임스페이스 리스트
            section(class="rooms")
                h1(class="title") ROOMS
                ul(id="room-list")
                //-    방 리스트

            section(class="chatting-room")
                div(class="header")

                    div(class="title")
                        h1(id="roomTitle" class="name") Cats
                        div(class="people-nums")
                            span 1
                            i(class="fas fa-user-alt")

                    span(class="welcome-msg") welcome 
                
                div(id="contents" class="messages")
                    //- 유저들의 메세지

                form(class="msg-form")
                    input(id="user-message" class="msg-input")

        nav(class="navbar") 
            a(href="/")
                i(class="fas fa-home" alt="main")    
            a(href="/user/info")   
                i(class="far fa-user" alt="user info")

